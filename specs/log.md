# Update Log

## 2026-08-18
* **Update**: Shipped 016 — `semantiql inspect` reads a datasource's catalogue with **no model required**, and the skill has Claude drive it: inspect, ask the analyst the handful of questions a schema cannot answer, write one YAML per table, loop on `doctor`. The correction that prompted it came from the user: the setup doc described hand-writing the model as *the* flow, when the agreed design was Claude discovering the database and writing the files itself. **`tables()` is the third widening of the adapter seam** after `close()` and `carries_timezone`, each found by building a real consumer — the seam is correct where something exercises it and merely unfalsified elsewhere. The server surface stays **two** tools: discovery uses a shell, not a third tool, so the enforcement boundary is the width it was. Which forced `02-architecture.md` to state the narrower true rule — Claude never writes the model *as a side effect of answering* — and to explain why building has a shell that asking deliberately lacks. 20 new tests, gate green both ways: 430 + 27 + 61 with Postgres up, 60 skipped with it down [016](/016-schema-discovery/spec.md)
* **Update**: Shipped 015 — a model may be a **directory** of YAML files, one per table, so a warehouse is a reviewable tree instead of a two-thousand-line document and a metric change is a small diff in an obvious place. Every merge ambiguity is refused naming the files, because last-one-wins across files is the same silent redefinition `_StrictLoader` already refuses within one. And the half that actually breaks at scale: `describe_model` now returns an **index** — names, descriptions, entity counts — with definitions on request, because a thirty-table model sent in full crowds out the conversation and makes a plausible measure from the wrong table likelier. One table still answers in full, since there is nothing to choose. 36 new tests, gate green at 340 + 27 + 58. `Table` gained a `description` as a recorded amendment: FR-8 promised one and the model had no field for it [015](/015-model-directory/spec.md)
* **Update**: Shipped 014 — a relocatable `.mcpb` bundle a colleague installs by opening it, with the semantic model chosen in a **file picker** and a Postgres DSN in a field marked secret, which is how "end users never touch a connection string" stops being aspirational. Built by a script the gate runs; the source travels inside so it needs no release, and swapping to a published version later is one function. Prompted by a defect the user found in 013: a plugin that derives the checkout from its own location cannot survive being unzipped elsewhere. **T5 found a bug that would have shipped** — the version was read from `importlib.metadata`, which raises inside a bundle, so the import aborted on any machine without SemantiQL already installed. Invisible in development, and the first attempt at that test *passed* because the venv was contaminated. 13 new tests including a regression for it [014](/014-desktop-bundle/spec.md)
* **Update**: Shipped 013 — a `plugin/` installing the MCP server and a skill together, and the architecture doc that finally describes what Claude knows as a component with a file rather than an opaque box at the top of a diagram. The skill carries what one `instructions` string cannot: worked examples, refusal-repair, and the N6 rule to report a missing definition and **stop**. `tests/test_plugin.py` pins the skill to the engine — the grains it teaches are parsed out of the prose and asserted equal to `_GRAINS`, because two statements of one rule in two files is how documentation drifts into teaching SQL the engine refuses. 23 new tests. **AC-2 is recorded as only partly met**: this repo cannot install a plugin into a client, so the install is a manual step with two open questions named, not a green tick [013](/013-plugin-and-skill/spec.md)
* **Update**: Shipped 012 — a local MCP server with exactly **two** read-only tools, `describe_model` and `query`, over stdio. That surface is the enforcement boundary: a shell-based skill would have been easier and would have let the model reach the database by any route. A refusal travels as a normal answer carrying its reason, which is the repair half of check-and-repair. 21 new tests, verify gate green at 288 + 27 + 58, **`engine/` untouched**. First consumer of `label` and `description`, which `docs/09` had recorded as written-but-never-read. Also fixed the hazard `adapters/postgres.py` predicted: a long-lived connection sat `idle in transaction`, pinning a snapshot — `execute` now ends the read-only transaction after fetching, with a test asserting the backend state because nothing else would catch it. **OQ-1: no mypy override needed** [012](/012-mcp-server/spec.md)
* **Update**: Clarified 8 ambiguities for 012 — five measured against the installed SDK and a live database, two of which contradicted the docs. A plain `dict` return leaves `structured_content` empty and forcing it is an outright error, so a refusal is carried by a **pydantic return type**, which also generates the tool's output schema. And FR-7's single connection would have been a regression as written: `autocommit=False` is what enforces read-only, and it leaves the connection `idle in transaction` — the exact case `adapters/postgres.py` says to revisit for a server. Ending the transaction after each answer gives one connection without pinning a snapshot, with read-only measured to survive it [012](/012-mcp-server/clarifications.md)
* **Creation**: Spec drafted for a local MCP server — the interface the product was designed for and the last MVP-critical gap. Semantic SQL was never meant for a human to write, so without an LLM in the loop the language has no author and the engine is finished for someone who cannot reach it. Also the first consumer of `label`/`description`, which `docs/09` records as written-but-never-read, and the prerequisite for the accuracy benchmark. 12 FRs; FR-10 keeps the path to remote as a constraint rather than a hope [012](/012-mcp-server/spec.md)

## 2026-08-17
* **Update**: Shipped 011 — a grain now truncates a **cast** column, or an `AT TIME ZONE` conversion when the dimension declares `timezone:`, so a month boundary no longer depends on the database server's setting. 22 new tests, verify gate green at 268 + 27 + 57. **T0 passed and found a hole the plan had not covered**: `AT TIME ZONE` is correct only over a genuinely zone-carrying column — on a naive `timestamp` it moves the bucket on both engines, and on a `date` DuckDB moves it while Postgres does not — so a well-meant `timezone:` would have introduced this spec's own fault. AD-3 gained a second doctor check covering the reverse direction. T3 turned out unnecessary, which is the backward-compatibility evidence: the e2e suite compares values, the cast is a no-op on DuckDB, and no number moved [011](/011-time-grain-timezones/spec.md)
* **Update**: Plan, tasks and validation drafted for 011; analyze clean. Clarify **contradicted the spec by measuring it**: this is not a Postgres-vs-DuckDB divergence — both engines bucket in the session timezone and both are wrong the same way, which means a differential suite is structurally blind to it. Design is a `CAST(… AS TIMESTAMP)` for the common case and an optional dimension `timezone:` emitting `AT TIME ZONE` for the tz-carrying one. AD-4 reverses clarification Q6 after reading `doctor.py`; AD-5 narrows FR-2 to "any model doctor passes" and is flagged as the decision most worth overruling. 15 files to modify, 2 to add [011](/011-time-grain-timezones/plan.md)
* **Creation**: Spec drafted for time-grain time zones — `DATE_TRUNC` buckets by the Postgres server's `TimeZone` setting, so one model over one dataset answers differently on two servers. Harmless on a `date` column; on a `timestamptz` column declared `type: date` — which nothing refuses and `doctor` actively blesses — revenue moves between months. Spec 007 deferred time zones deliberately and that deferral has come due. FR-3 closes a gap in 010: the differential suite compares `month` and no other grain, so "the engines agree" is evidence about one of five [011](/011-time-grain-timezones/spec.md)
* **Update**: Shipped 010 — 13 of 13 tasks; a Postgres adapter, `--datasource`/`--dsn` on the CLI, and a differential suite asserting both engines answer one model identically. **AC-9, the N4 verdict: zero files changed under `engine/`.** 76 new tests, verify gate green at 252 + 27 + 24. Two findings only a second adapter could surface: `close()` was missing from the `Adapter` Protocol (added, AD-8), and `DATE_TRUNC` returns a timezone-aware value on Postgres from byte-identical SQL — pinned by a test and carried forward as its own spec. A third was caught by measuring rather than assuming: `read_only` is silently ignored under `autocommit`, so `autocommit=False` is load-bearing for N5 and now has a test saying so [010](/010-postgres-adapter/spec.md)
* **Update**: Task list drafted for 010 — 14 tasks in five phases, two `[P]` groups. Group 2 names `CLAUDE.md` as the one near-overlap and excludes it, because it is a symlink to `AGENTS.md` and editing both would be the same file twice [010](/010-postgres-adapter/tasks.md)
* **Update**: Plan and impact map drafted for 010 — one new adapter module, an `_open_adapter` seam in the CLI, and a `pg`-marked differential suite; **zero files under `engine/`**. A live psycopg 3.3.4 probe found the trap: `types.get()` accepts an array OID and returns the *element* type with no flag, so `integer[]` would classify as `number` — the same bug 009 found in DuckDB's `INTEGER[]`, arriving by a route the DuckDB test cannot see. 9 files to modify, 5 to add [010](/010-postgres-adapter/plan.md)
* **Update**: Clarified 8 ambiguities for 010 — psycopg 3 (it ships `py.typed`, so it needs no strict-mode exemption where `duckdb` did); a hard dependency rather than an extra; explicit `--datasource` because inferring it from the model would make `run`'s dialect-mismatch refusal unreachable; the ten-row retail corpus rather than TPC-H; a CSV `source` refused on Postgres rather than passed through as a table name; and a CI service container, which needs no secret because its credentials are literals in the workflow [010](/010-postgres-adapter/clarifications.md)
* **Creation**: Spec drafted for a Postgres adapter — N4 is verified for imports and one transpile assertion, both of which a single-adapter repo satisfies by accident; every spec since 004 has added a DuckDB-shaped decision at the seam. 12 FRs, with FR-9 stating "zero files changed under `engine/`" as a falsifiable check [010](/010-postgres-adapter/spec.md)
* **Update**: Shipped 009 — `semantiql doctor` checks a model against its database in one pass; `Adapter.columns` now takes a `source`, builds its probe through `relation()` instead of interpolating, and classifies types into the model's vocabulary. 21 new tests, verify gate green at 213 + 27. Writing the classifier's test found two real bugs in it — `INTERVAL` matched the `INT` prefix and `INTEGER[]` read as a number [009](/009-doctor/spec.md)
* **Creation**: Plan, tasks and validation drafted for 009 — `Adapter.columns` becomes source-based and typed, a `doctor` module sits outside `engine/`, and the CLI gains the verb plus `--database`. 10 files to modify, 2 to add [009](/009-doctor/plan.md)
* **Update**: Clarified 5 ambiguities for 009 — `Adapter.columns` changes to take a `source` and build via `relation()` rather than interpolating; the adapter classifies its own column types; `--database` added; report-only, no `--fix`; the sample-questions half of the docs' promise is documented as still missing [009](/009-doctor/clarifications.md)
* **Creation**: Spec drafted for `semantiql doctor` — nothing checks a model against reality today, and `type:` now decides how filters behave [009](/009-doctor/spec.md)

## 2026-08-16
* **Update**: Shipped 008 — end-to-end suite over a locally generated TPC-H corpus: 27 tests checking the engine against hand-written SQL, plus an edge table for the nulls, booleans and zero divisors TPC-H lacks. Verified scale-independent at sf=0.1. **Checkpoint 3 did not fire** — the run went from the plan straight into implementation, so `tasks.md` is a record written afterwards and carries no attestation [008](/008-e2e-suite/validation.md)
* **Creation**: Plan and impact map drafted for 008 — one session fixture generates TPC-H into a temporary DuckDB file, denormalises it to a single view, and hands tests a read-only adapter. 4 files to modify, 5 to add [008](/008-e2e-suite/plan.md)
* **Update**: Clarified 5 ambiguities for 008 — TPC-H via `dbgen` over a download or a bespoke generator; hand-written physical SQL as the oracle rather than pinned figures; on by default at sf=0.01 with a marker; skip offline; a companion table for the nulls and booleans TPC-H provably lacks [008](/008-e2e-suite/clarifications.md)
* **Creation**: Spec drafted for an end-to-end suite over a large dataset — seven specs have shipped against a ten-row corpus where a month holds eight rows [008](/008-e2e-suite/spec.md)

## 2026-08-16
* **Update**: Shipped 007 — 5 of 5 tasks; `DATE_TRUNC` over date dimensions with a closed five-grain vocabulary, the extract forms refused for collapsing years, 18 new test cases, verify gate green at 194 tests. One canonical statement now renders four distinct ways, the strongest N4 evidence in the repo [007](/007-time-grains/spec.md)
* **Creation**: Plan and task list drafted for 007 — grain carried on `Projection`, GROUP BY repeats the expression, extract forms refused with a message naming `DATE_TRUNC`. 7 files to modify, 0 to add [007](/007-time-grains/plan.md)
* **Update**: Clarified 4 ambiguities for 007 — grain in the request rather than predeclared in the model; `DATE_TRUNC` only, with `MONTH()`/`EXTRACT()` refused because they collapse years; default output name `<dimension>_<grain>`; GROUP BY the expression, not the alias [007](/007-time-grains/clarifications.md)
* **Creation**: Spec drafted for time grains — the last Tier-1 report gap, and the strongest N4 demonstration available since `DATE_TRUNC` renders five ways across dialects [007](/007-time-grains/spec.md)

## 2026-08-16
* **Update**: Shipped 006 — 7 of 7 tasks; metrics derived from measures with a closed grammar validated at load, every divisor guarded with NULLIF because DuckDB returns `inf` for 1/0, 19 new test cases, verify gate green at 176 tests. Layer 1 now has the metrics its own architecture doc has always described [006](/006-derived-metrics/spec.md)
* **Update**: Task list drafted for 006 — 7 tasks, one [P] pair for the derived copies [006](/006-derived-metrics/tasks.md)
* **Creation**: Plan and impact map drafted for 006 — new `knowledge/expression.py` holds the metric IR and its closed-grammar parser; metrics validate at load and compile to guarded aggregate expressions. 10 files to modify, 1 to add [006](/006-derived-metrics/plan.md)
* **Update**: Clarified 4 ambiguities for 006 — expression string over structured YAML; guard every divisor because DuckDB returns `inf` for 1/0 while Postgres raises; measures only, no metric-in-metric; validate at load, not at query time [006](/006-derived-metrics/clarifications.md)
* **Creation**: Spec drafted for derived metrics — ratios defined in the model, computed after grouping, closing the gap between layer 1's description and the code [006](/006-derived-metrics/spec.md)

## 2026-08-16
* **Update**: Shipped 005 — 6 of 6 tasks; ORDER BY over projected names, LIMIT/OFFSET as whole numbers, 25 new test cases, verify gate green at 157 tests. The transpile tripwire was replaced by a real T-SQL-vs-DuckDB assertion, as its own docstring instructed — N4 is now verified for behaviour, not only for imports [005](/005-order-and-limit/spec.md)
* **Update**: Task list drafted — 6 tasks, one [P] pair for the derived copies [005](/005-order-and-limit/tasks.md)
* **Creation**: Plan and impact map drafted — extend `_SELECT_ARGS` to order/limit/offset, order by the output alias, and replace the transpile tripwire with a real T-SQL-vs-DuckDB assertion. 7 files to modify, 0 to add [005](/005-order-and-limit/plan.md)
* **Update**: Clarified 4 ambiguities — order only by projected names; honour `nulls_first` as recorded; LIMIT/OFFSET are whole-number literals; `LIMIT ALL` passes through as "no limit" because that is what it means [005](/005-order-and-limit/clarifications.md)
* **Creation**: Spec drafted for ordering and limits — ORDER BY restricted to projected names, LIMIT/OFFSET as non-negative integer literals, and FR-8 converts the transpile tripwire into a real assertion now that LIMIT renders as TOP on T-SQL [005](/005-order-and-limit/spec.md)
* **Update**: Shipped 004 — 8 of 8 tasks; `WHERE` over dimensions with typed literals, predicate rebuilt from a neutral IR, 42 new test cases, verify gate green at 132 tests. Two pre-existing tests asserting `WHERE` is refused were rewritten to the new contract, disclosed as an amendment [004](/004-filter-by-dimension/spec.md)
* **Update**: Task list drafted — 8 tasks, one [P] pair for the derived copies [004](/004-filter-by-dimension/tasks.md)
* **Creation**: Plan and impact map drafted — validate the WHERE into a neutral predicate IR, rebuild it in the compiler from the model. AST probe found `NOT LIKE` is `Like(negate=True)`, a scalar flag whose loss would invert a filter silently. 7 files to modify, 0 to add [004](/004-filter-by-dimension/plan.md)
* **Update**: Clarified 5 ambiguities — neutral IR over AST reuse; explicit `CAST(… AS DATE)`; `type:` becomes enforced rather than declarative; both negation representations handled; `NOT IN` NULL behaviour documented rather than refused [004](/004-filter-by-dimension/clarifications.md)
* **Creation**: Spec drafted for dimension filters — WHERE over dimensions with a fixed predicate set, literals typed against the dimension's declared `type`, predicate rebuilt from the model. Request contract stays SQL text (user decision, 2026-08-16): grow the subset rather than move to a structured request [004](/004-filter-by-dimension/spec.md)

## 2026-08-15
* **Update**: Amended mid-implement — self-review found the same silent-drop class inside this change's own fix: `walk()` yields only expression nodes, so `FROM ONLY orders` and `WITH ORDINALITY` (stored as bare flags) were still accepted and dropped. `_FROM_NODES` became `_FROM_NODE_ARGS`, checking arguments as well as node types; plan gains architecture decision 5, tasks gain T8 [003](/003-refuse-unimplemented-constructs/plan.md)
* **Update**: Shipped 003 — 8 of 8 tasks; `_UNSUPPORTED_CLAUSES` replaced by `_SELECT_ARGS` + `_FROM_NODE_ARGS` allowlists, 19 new test cases, verify gate green at 90 tests. All 6 ACs met; the ignored column qualifier (Q3) carried forward as a follow-up [003](/003-refuse-unimplemented-constructs/spec.md)
* **Update**: Task list drafted — 7 tasks, one [P] pair for the two derived copies (coverage map, agent brief) [003](/003-refuse-unimplemented-constructs/tasks.md)
* **Creation**: Plan and impact map drafted — invert to two allowlists (`Select` args, `FROM`-subtree node types) derived from an AST probe over sqlglot 30.17.0; the enumerated tuple survives as wording only. 4 files to modify, 0 to add [003](/003-refuse-unimplemented-constructs/plan.md)
* **Update**: Clarified 4 ambiguities — invert the check to default-refuse rather than extend the known-bad list; keep every request answered today answered; the ignored column qualifier is a separate, lesser issue; rules stay in `engine/validate.py` [003](/003-refuse-unimplemented-constructs/clarifications.md)
* **Creation**: Spec drafted for closing the silent-drop gap — `TABLESAMPLE` and `PIVOT`/`UNPIVOT` validate today and are discarded at compile time; FR-3 makes refusal the default for anything unimplemented rather than an enumerated list. T2, forced by the query-validation layer being a trust-boundary artifact [003](/003-refuse-unimplemented-constructs/spec.md)
* **Update**: Published to a public remote after a clean pre-push audit; ownership commentary and work addresses redacted from the change records first [001](/001-init-project-scaffold/spec.md)
* **Update**: Implemented 001 autonomously — 15 of 16 tasks; working Python package, 23 tests, verify gate green on 3.11 and 3.13. T14/AC-9 blocked on the missing conduct address; AC-6/8/12 partial pending a remote [001](/001-init-project-scaffold/tasks.md)
* **Creation**: Plan and impact map drafted — src-layout package with one module per architectural layer, `adapters/base.py` as the N4 seam, one verify script CI also calls. 4 files to modify, 27 to add. Three open questions for gate 2, the live one being that `uvx semantiql` cannot work before PyPI [001](/001-init-project-scaffold/plan.md)
* **Creation**: Clarify resolved four questions — Python+uv runtime, GitHub-only security reporting (5-day ack), a personal CoC contact (value still pending), weekly-triage no-SLA review. FR-8..FR-11 made concrete; phase moves to planning [001](/001-init-project-scaffold/clarifications.md)
* **Update**: Gate 1 passed — spec approved by human:thuan.le, recorded as a `verified` entry; phase moves to clarifying [001](/001-init-project-scaffold/spec.md)
* **Update**: Scope confirmed — 001 stays a single spec covering all 15 FRs; the three-way split was considered and declined because the setup, CI, and quickstart commands are the same commands [001](/001-init-project-scaffold/spec.md)
* **Update**: Added three engineer comprehension stories and FR-14 (layer→module orientation, adapter seam) and FR-15 (positioning vs Cube, dbt Semantic Layer, MetricFlow, Malloy) [001](/001-init-project-scaffold/spec.md)
* **Update**: Runtime decided — Python 3.11+ with uv, closing the constitution's open tech-stack question; sqlglot being Python-only was the forcing constraint. Adds FR-13 to 001 for correcting the stale `npx semantiql init` in README and docs [constitution](../.specify/memory/constitution.md)
* **Update**: Folded the open-source artifacts into 001 — FR-8..FR-12 (SECURITY, CoC, CONTRIBUTING, README support line, issue/PR templates); FR-6 tightened to require fork-PR CI without secrets. Public push recorded as out of scope, gated on licensing confirmation [001](/001-init-project-scaffold/spec.md)
* **Creation**: Spec drafted for repo initialization, gate 1 pending [001](/001-init-project-scaffold/spec.md)
* **Creation**: Bundle opened — `index.md` and `log.md` at `specs/`

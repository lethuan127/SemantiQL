"""The test suite, laid out to mirror `docs/07-code-map.md`.

A package rather than a loose directory, so subdirectories can share `tests._support` and two
files in different layers may carry the same name without pytest's import machinery objecting.
"""
